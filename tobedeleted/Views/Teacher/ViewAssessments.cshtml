@model IEnumerable<tobedeleted.Models.Assignment>

@{
    ViewBag.Title = "Index";
    Layout = "~/Pages/_LayoutTeacher.cshtml";

}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@*<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AssignTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AssignType)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AssignTitle)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AssignType)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.AssignID }) |
                @Html.ActionLink("Details", "Details", new { id = item.AssignID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.AssignID })
            </td>
        </tr>
    }

</table>*@
